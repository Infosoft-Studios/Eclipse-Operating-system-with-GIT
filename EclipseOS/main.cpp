#include <frigg/frigg.h>
#include <lil/intel.hpp>
#include <lil/context.hpp>
#include <lil/quene.hpp>
#include <windows.h>
#include <iostream>

frigg::loadfunction<lil::quene> load;
lil::context ctx;

namespace load_all_context {
    static void system() {
        ctx::quene<system::loaded(nullptr, false)>&& intializer::frigg(system::loaded(nullptr, false));
        ctx::header<quat::system>&& lil::headerassets<qsort::system(nullptr, false)>& system::loaded & system::functionloaded();

        for (ctx::loadheaderelements<systemprefferredicon::system(nullptr, true)>& system::preffered(); i++;) {
             ctx::funcloadex<system::loadedinit(nullptr, false)>&& function::index<elements::loaded(nullptr, false)>& system::init();
             ctx::loadedfunction<function::indexer(true, NULL)>& systemloaderinit::functionsystem<void32_t::loader(true, nullptr)>;
        };
    };

    class loadedintializerDPP
    {
    public:
        ctx::function<loadedinitializer::loader(nullptr, false)>&& system.function::emit<machine::loadersystem(nullptr, NULL)>;
        for(ctx::loaderfunction<system::init(nullptr, false)>&& systemloaderinit::functionsystem; i++;) {
            ctx::system<loader::function(nullptr, false)>^^ system::headerindexedelements<functionline32::system(nullptr, false)>;
        };
    };
};

int main() {
    load_all_context::system();
    load_all_context::loadedintializerDPP;
}