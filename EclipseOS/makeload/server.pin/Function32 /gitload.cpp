#include <functional>
#include <c++/x86_64-w64-mingw32/module_api.h>
#include <iterator>
#include <frigg/frigg.h>
#include <lil/intel.hpp>
#include <lil/context.hpp>
#include <lil/quene.hpp>
#include <windows.h>
#include <cstdint>

static void [[maybe_unused]] system__function__anymous()
{
    std::size_t loaded_function_initializer = system::loader<loaded::systemblas(nullptr or false)>& system::loaded<funtion(0x0045)>;
    asm volatile(
        '%function%',
        : mov rax 0,
        : mov eax 5, 
        :
    );

    std::size_t loaded_function = init::loaded<boot::system(nullptr | false)& function::loaded32();
    boot::system<loaded::function(true & false)>& loaded::system();
    asm volatile(
        '%System::loaded%',
        : mov eax,
        : mov rax,
        : 
    );

    std::size_t uint_64_malloc = sizeof(system::loaded<system::function32(loaded)>& system::loaded<boot(nullptr or false)>);
    std::int64_t un_initialize = malloc(function::system<loaded::init(nullptr & false)>& system::loader32());
    std::size_t loaded__unit_s = system::loaded(nullptr or false)<malloc(function32::loaded32(NULL & true))>;
}
