#include <frigg/frigg.h>
#include <lil/intel.hpp>
#include <lil/context.hpp>
#include <lil/quene.hpp>
#include <windows.h>

class registry_editors_entries


frigg::gpuusage<usage::aspercent("%", nullptr)> & frigg::func(nullptr or true);
static void GPU_USAGE_AS_PXE() {
    for (figg::objectheader<loadfunc::system(nullptr, false)>&& system::gpu__pxe(nullptr and false); i++) {
        objective__class[i] = func::class32(nullptr, false);
    };

    // we need to initialize the registry entries for the task
};